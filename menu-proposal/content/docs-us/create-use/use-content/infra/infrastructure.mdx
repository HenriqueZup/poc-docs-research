---
title: Create and Publish Infrastructure via STK CLI
description: How to create Shared Infra via STK CLI.
---

> In this section, you will find a guide on how to create and publish an Infrastructure via STK CLI..

<br/>

To create Infrastructure on StackSpot, you can use Infrastructure Plugins that generate infrastructure pieces (IAC code). This process is similar to creating an [**Application**](create-use/use-content/application/index.md), which generates source code from Application Plugins.

## See the steps

import ThemedImage from '@theme/ThemedImage';
import useBaseUrl from '@docusaurus/useBaseUrl';

<ThemedImage alt="Criar uma aplicação via o Portal da StackSpot." width="auto" sources={{ light: useBaseUrl('/img/diagrams/CREATE-INFRA-EN-light.png'), dark: useBaseUrl('img/diagrams/CREATE-INFRA-EN-dark.png'), }} />

### **Requirements**

- [**STK CLI installed**](home/stk-cli/commands/index.md) and a Workspace member.
- You must be **logged in to** your StackSpot Account.
- Access a [**Studio**](create-use/studio/create-studio.md).  
- You need a [**Stack and a Starter**](create-use/create-content/stacks/create-stack.md).
- You must have [**environments on your account**](home/account/organization/create-env.md)
- [**Must have configured the Deployment via Self-Hosted to set up and connect the environments in the Cloud Account**](deployment/deploy/overview.md)
- You must have [**your SCM configured**](home/account/organization/scm-integration/index.md) (Your **Account administrator** is responsible for this configuration).
- If your account is Enterprise and the administrator decides that users should **manage access to <Tooltip text="Source code management (SCM) tracks changes to a code repository, keeping a record of alterations and assisting in managing conflicts that arise when integrating updates from different contributors.">SCM</Tooltip> via Personal Access Token (PAT) individually**. You must configure it in [**'External Connections'**](home/account/profile/external-connections.md).

<br/> 

**If you still need to meet these requirements, click the links to complete them before proceeding.**

## **Follow the steps to create your Shared Infra via STK CLI**

<br/>

> You will follow the steps below via [**STK CLI**](home/stk-cli/install.md).

<br/>

**Step 1.** Open your terminal, select the Workspace you want to use for Infrastructure and execute:

```bash
stk use workspace
```

Choose the Workspace from the list and press Enter.

:::tip
To see the list of available workspaces from your terminal, run the command [**`stk list workspace `**](home/stk-cli/commands/workspace-commands.md).
Your workspace is marked with a star *.

You can also see the list on the [**StackSpot Platform**](https://app.stackspot.com/login) in the **'Workspaces** section.
:::

**Step 2.** To create an Infrastructure, execute:

```bash
stk create infra
```

Answer the questions on the terminal:

- **Name your Infrastructure**: Enter the name of your Infrastructure;

> - The Infrastructure name has a **60-character** limit.

- **Do you want to start a git repository?**: Answer **'(Y/n) Yes or No'** to start a Git repository in the Infrastructure folder. If the answer is **'(Y) Yes'**, then you must enter the URL of the remote repository;
- **Infrastructure Description**: Enter a brief description for your Infrastructure;
- **Select the Studio**: Select one of the Studios available in your account;
- **Select Stack**: Select the desired Stack;
- **Select the version of the Stack**: Select the version of the Stack you want to use.
- **Select the desired infrastructure Plugins:** Select the desired infrastructure plugins from the list displayed in the terminal;

- **Name the use of the Plugin:** Plugin usage is automatically filled. You must then answer the Plugin inputs.

:::caution

You **can apply the same Plugin more than once in your Infrastructure**. That's why the question below appears every time:

**`? Name the usage of the plugin (studio/stack/name-of-plugin)`**

It is the [**`alias`**](create-use/create-content/plugin/v2-migration-guide.md) of the Plugin's usage, and it comes already filled out as default input by the STK CLI. For example:

- The name of the Plugin is **bucket-s3**
- If it is the first time you apply this Plugin, the alias should be the **bucket-s3-1694809616257** name. This number is a **[Unix Timestamp](https://unixtime.org/)**.

The **`alias`** indicates the purpose of this first Plugin application and avoids duplicate names.

**The `alias` is immutable once configured. If you want to edit this name, this is the only time you can.**

::: 

**Step 3.** Answer the questions in your terminal. If any Plugin requires a [**Connection Interface**](create-use/connections/connection-interface.md), provide the usage's **`alias`**.

<br/>

Please just repeat the process for all the Plugins you apply.

<br/>

You created your Infrastructure!

<br/>

:::danger

You cannot manually edit the **`stk.yaml`** file of an Application or Infrastructure should. You can modify the Plugins in your Application or Infrastructure. To do so, use the following commands:

- To modify the version or settings of a Plugin:

```bash
stk update plugin
```

- To remove a Plugin:

```bash
stk remove plugin
```

- To add new features to your project:

```bash
stk apply plugin
```

:::

**Step 4.** Register your Shared Infra into the account. Access its directory:

```bash
cd <shared-infra-name> 
```

Then, execute the command below to register:

```bash
stk register infra
```

Answer the questions:

- **Repository URL**: Inform the repository that will store your application's code;
- **Base branch**: Inform the branch you will use in the repository.

<br/>

Your Shared Infra was created and registered.

<br/>

Check all the steps above:

<AsciinemaPlayer src="/videos/create-shared-infra.cast " autoplay="true" loop="true" speed="2"/>

You can now view your Infrastructure data within your Workspace on the [**StackSpot Platform**](https://app.stackspot.com/login). Access the **'Infrastructure’** section.

:::caution 

The **infrastructure deployment** is only performed at this stage if your organization has [**configured the deployment via Runtime Self-Hosted**](deployment/deploy/self-hosted-runtime.md).

:::

Here is the translation of your text into English:

## **How to Use Account Variables to Configure Plugins in Your Application**

You can choose to [**use Custom Variables created by your Organization**](home/account/organization/variables/create-variables.md) to fill in the inputs for Plugins and Actions when creating your Infrastructure.

To do this, type `{{` when responding to the input and select the Variable you want to use.

[**Visit the How to Create Account Variables page to learn more.**](home/account/organization/variables/create-variables.md)


## **Next step**

- Learn how to deploy your Infrastructure, [**in the Deployment section**](deployment/deploy/overview.md).
- Manage how you [**run Actions in your Infrastructure**](create-use/use-content/application/actions-application.md).
- [**Manage and monitor your Infrastructure**](deployment/manage/activities.md).
- To **destroy** your Infrastructure [**see the Destroy section**](deployment/deploy/register-app-destroy.md).
